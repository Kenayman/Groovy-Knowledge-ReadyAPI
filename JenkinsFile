pipeline {
    agent any

    stages {

        stage('Run tests') {
            steps {
                sh 'mvn clean test'
            }
        }

        stage('Generate Allure Report') {
            steps {
                allure results: [[path: 'target/allure-results']]
            }
        }
    }
}