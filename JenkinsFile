allure includeProperties: false, jdk: '', resultPolicy: 'LEAVE_AS_IS', results: [[path: 'allure-report']]

pipeline {
    agent any
    stages {
        stage('build') {
            steps {
                bat 'whoami'
            }
        }
    }
}