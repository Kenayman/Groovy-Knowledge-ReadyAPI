pipeline {
    agent any

    stages {

        stage('Run Tests') {
            steps {
                sh 'mvn clean test'
            }
        }

    }

    post {
        always {
            allure includeProperties: false,
                   jdk: '',
                   resultPolicy: 'LEAVE_AS_IS',
                   results: [[path: 'reports/allure-results']]
        }
    }
}